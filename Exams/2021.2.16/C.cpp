#include <cstdio>
using namespace std;

int main() {
    int n;
    scanf("%d", &n);
    int op, x, k, v;
    while (n--) {
        scanf("%d", &op);
        switch (op) {
            case 1:
                scanf("%d", &x);
        }
    }
}
