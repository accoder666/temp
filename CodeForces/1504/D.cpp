#include <cstdio>
using namespace std;

const int maxn = 110;
int inline void put(const int&, const int&, const int&);

int main() {
    int n;
    scanf("%d", &n);
    int lim = n * n;
    int a;
    while (lim--) {
        scanf("%d", &a);
    }
}

inline void put(const int& b, const int& i, const int& j) {
    printf("%d %d %d\n", b, i, j);
    fflush(stdout);
    return;
}
