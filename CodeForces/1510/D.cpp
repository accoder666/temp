#include <cstdio>
using namespace std;

const int maxn = 100010;
int a[maxn], b[10];

int main() {
    //    for (int i = 0; i < 10; i++) {
    //        for (int j = 0; j < 10; j++) printf("%d ", (i * j) % 10);
    //        printf("\n");
    //    }
    int n, d;
    scanf("%d %d", &n, &d);
    for (int i = 1; i <= n; i++) scanf("%d", a + i);
    return 0;
}
